# Advisory Index

This directory contains one Markdown advisory per assigned CVE summary.

Two separate advisory pages currently use `CVE-2026-37231` because the CVE
assignment response for service request 2003793 listed two separate summaries
under that same CVE ID. The pages intentionally preserve the assignment text
instead of inventing an unassigned CVE ID.

